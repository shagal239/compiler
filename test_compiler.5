int doit(int x, int y) {
    println(x + y);
    return x + y;
}

void main() {
    for (int i = 0; i < 10; i += 1) {
        for (int j = 0; j < 10; j += 1) {
            println(doit(i,j));
        }
    }
}